{
  "name": "proyectokpml-pipeline",
  "version": "1.0.0",
  "description": "ML training data pipeline for medical bill detection",
  "scripts": {
    "generate": "bash scripts/generate.sh",
    "render": "node scripts/render_bills.js",
    "prepare": "source .venv/bin/activate && python3 scripts/prepare_dataset.py",
    "train": "source .venv/bin/activate && python3 scripts/train.py",
    "export": "source .venv/bin/activate && python3 scripts/export_onnx.py",
    "infer": "node models/infer.js",
    "pipeline": "bash scripts/generate.sh && node scripts/render_bills.js && source .venv/bin/activate && python3 scripts/prepare_dataset.py && python3 scripts/train.py && python3 scripts/export_onnx.py"
  },
  "dependencies": {
    "csv-parse": "^5.5.6",
    "onnxruntime-node": "^1.20.1",
    "pdfkit": "^0.15.0"
  }
}
