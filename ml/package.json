{
  "name": "proyectokpml-pipeline",
  "version": "1.0.0",
  "description": "ML training data pipeline for medical bill detection",
  "scripts": {
    "generate": "bash scripts/generate.sh",
    "render": "node scripts/render_bills.js",
    "prepare": "python3 scripts/prepare_dataset.py",
    "pipeline": "bash scripts/generate.sh && node scripts/render_bills.js && python3 scripts/prepare_dataset.py"
  },
  "dependencies": {
    "csv-parse": "^5.5.6",
    "pdfkit": "^0.15.0"
  }
}
